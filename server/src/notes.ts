// 1) 

// import { Server } from "socket.io";
// const io = new Server();
// io.listen(8080);

// i) Socket.IO creates its own internal HTTP server
// ii) You cannot control that HTTP server
// iii) No callback, no middleware, no REST APIs
// iv) Good only for quick demos / practice
// v) instead use the method shown in code
